// Arrays, map & Object.key

carrito = ['Producto 1', 'Producto 2', 'Producto 3'];

/* let html= ``;
carrito.forEach( producto => {
     html += `<li>${producto}</li>`;   
});

const contennedor= document.querySelector('#app');
contennedor.innerHTML=html; */

/* let otra=carrito.map( producto => {
    return `El producto es: ${producto}`;
}); */

persona = {
    nombre: 'Pieda',
    apellido: 'Solorzano'
}

console.log(Object.keys(persona));